function isColorValid(color) {
  return color === "blue" || color === "green";
}

const isColorValid_2 = (color) => color === "blue" || color === "green";

console.log(
  isColorValid('blue'),
  isColorValid('')
);
